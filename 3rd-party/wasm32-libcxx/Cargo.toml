[package]
edition = "2024"
name = "wasm32-libcxx"
version = "0.1.0"
links = "wasm32-libcxx"
description = "LLVM's libc++ customized for WASM, sourced from the emscripten LLVM fork."
readme = "README.md"
keywords = ["libcxx"]
license = "BSD-3-Clause AND ISC AND MIT"
repository = "https://github.com/emscripten-core/emscripten"

[dependencies]
wasm32-libc = { path = "../wasm32-libc" }

[build-dependencies]
cc = { workspace = true }
